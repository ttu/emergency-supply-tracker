#!/bin/sh
set -e

# Format and lint staged files
npx lint-staged

# Type-check all files (source, tests, stories - catches type errors early)
npm run type-check:all

# Run tests (frequent feedback for agents)
npm test

# Build check (ensures code compiles)
npm run build
